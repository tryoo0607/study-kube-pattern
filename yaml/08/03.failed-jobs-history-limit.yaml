apiVersion: batch/v1
kind: CronJob
metadata:
  name: hello-fail-limit
  namespace: test
spec:
  schedule: "*/1 * * * *"   # 매 분 실행
  failedJobsHistoryLimit: 1   # 최근 실패한 Job 1개만 보관
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: hello
            image: busybox:1.28
            command:
            - /bin/sh
            - -c
            - exit 1   # 무조건 실패하게 설정
          restartPolicy: OnFailure